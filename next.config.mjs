/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['cdn1.iconfinder.com',"www.freepnglogos.com",  "images.saymedia-content.com", "images.pexels.com", "encrypted-tbn0.gstatic.com", "cdn.freebiesupply.com"] // it is allow to user show where it images exists in website and hostname as well
    }
};

export default nextConfig;
